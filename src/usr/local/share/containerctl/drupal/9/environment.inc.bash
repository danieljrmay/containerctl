# Drupal 9 environment variables and their default values

: "${DRUPAL9_VERSION:=9.4.1}"
: "${HOST_CUSTOM_MODULES_DIR:=$(realpath ~/Projects/Drupal/9/Modules)}"
: "${HOST_CUSTOM_THEMES_DIR:=$(realpath ~/Projects/Drupal/9/Themes)}"
