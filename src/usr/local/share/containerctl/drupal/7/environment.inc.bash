# Drupal 7 environment variables and their default values

: "${DRUPAL7_VERSION:=7.89}"
: "${HOST_CUSTOM_MODULES_DIR:=$(realpath ~/Projects/Drupal/Modules)}"
: "${HOST_CUSTOM_THEMES_DIR:=$(realpath ~/Projects/Drupal/Themes)}"
