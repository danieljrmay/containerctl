# Backdrop environment variables and their default values

: "${BACKDROP_VERSION:=1.22.0}"
: "${HOST_CUSTOM_MODULES_DIR:=$(realpath ~/Projects/Backdrop/Modules/custom)}"
: "${HOST_CUSTOM_THEMES_DIR:=$(realpath ~/Projects/Backdrop/Themes/custom)}"
