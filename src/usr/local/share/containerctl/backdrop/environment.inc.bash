# Backdrop environment variables and their default values

: "${BACKDROP_VERSION:=1.21.4}"
: "${HOST_CUSTOM_MODULES_DIR:=$(realpath ~/Projects/Backdrop/Modules)}"
: "${HOST_CUSTOM_THEMES_DIR:=$(realpath ~/Projects/Backdrop/Themes)}"
